import React, {Component} from 'react';
import './stylesheets/header.scss'

export default class Header extends Component {
    render() {
        return (
        <h1 className='header'>RAPID CITY</h1>
        );
    }
}